import SectionTitle from './ui/SectionTitle';

// interface LatestReleasesProps {
//   // Add your props here
// }

const StoreSection: React.FC = () => {
  return (
    <section>
      <SectionTitle title="Store" buttonTitle="View all products"  />
    </section>
  );
};

export default StoreSection;